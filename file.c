#include <math.h>

void f(char * restrict joe){}

int to_diff(double k,double l)
{
    unsigned int long c = 4;
    int p = sin(k)*cos(k)+pow(k,2);
    return 0;
}

int main(){
    double m = 4.0;
    to_diff(m,m);
}
